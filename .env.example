# Flux Ops Center SPCS - Environment Configuration
# Copy this file to .env and update values for your environment

# =============================================================================
# SNOWFLAKE DATABASE CONFIGURATION
# =============================================================================

# Target database name
SNOWFLAKE_DATABASE=FLUX_DB

# Compute warehouse for queries
SNOWFLAKE_WAREHOUSE=FLUX_WH

# Connection name for Snowflake CLI tools
SNOWFLAKE_CONNECTION=your_connection_name

# =============================================================================
# SNOWFLAKE POSTGRES (Required for map visualization)
# =============================================================================
# Create a Postgres instance using scripts/sql/05_postgres_setup.sql
# Save credentials shown after CREATE POSTGRES INSTANCE - they cannot be retrieved!

VITE_POSTGRES_HOST=<your_instance>.<region>.postgres.snowflake.app
VITE_POSTGRES_PORT=5432
VITE_POSTGRES_DATABASE=postgres
VITE_POSTGRES_USER=<your_username>
VITE_POSTGRES_PASSWORD=<your_password>

# =============================================================================
# CORTEX AGENT (Required for Grid Intelligence Assistant)
# =============================================================================
# Configure which Cortex Agent to use for the Grid Intelligence chat feature.
# Users must ensure the agent exists and is accessible to the application.

# Agent location - defaults to SNOWFLAKE_INTELLIGENCE.AGENTS.GRID_INTELLIGENCE_AGENT
CORTEX_AGENT_DATABASE=SNOWFLAKE_INTELLIGENCE
CORTEX_AGENT_SCHEMA=AGENTS
CORTEX_AGENT_NAME=GRID_INTELLIGENCE_AGENT

# Authentication (for local development only):
# In SPCS: OAuth token is auto-provided at /snowflake/session/token
# For local dev: Create a Personal Access Token (PAT) in Snowsight UI
#   Admin → Users & Roles → Your User → Personal Access Tokens

SNOWFLAKE_PAT=<your_personal_access_token>
SNOWFLAKE_HOST=<your_account>.snowflakecomputing.com

# =============================================================================
# AWS CONFIGURATION (Prevents boto3 SSO errors)
# =============================================================================
# The snowflake-connector-python imports boto3 which reads ~/.aws/config
# These settings prevent it from using expired SSO tokens

AWS_PROFILE=
AWS_SDK_LOAD_CONFIG=false
AWS_CONFIG_FILE=/dev/null

# =============================================================================
# SPCS DEPLOYMENT (Optional - for quickstart.sh)
# =============================================================================

# SNOWFLAKE_ACCOUNT=your_account
# SPCS_IMAGE_REGISTRY=your_account.registry.snowflakecomputing.com
# COMPUTE_POOL=FLUX_OPS_CENTER_POOL

# =============================================================================
# DEBUGGING
# =============================================================================

# Enable debug logging
# FLUX_DEBUG=1
